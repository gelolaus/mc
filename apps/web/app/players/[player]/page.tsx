import { notFound } from 'next/navigation'; import Image from 'next/image'; import { api, avatar, type Player } from '../../../lib/api'; import { date, duration } from '../../../lib/format'; import type { Metadata } from 'next';
export async function generateMetadata({params}:{params:Promise<{player:string}>}):Promise<Metadata>{const {player}=await params;return {title:`${player} — JPCS-APC Minecraft`}}
export default async function PlayerPage({params}:{params:Promise<{player:string}>}){const {player:id}=await params;const player=await api<Player>(`/players/${encodeURIComponent(id)}`);if(!player)notFound();return <section className="profile"><div className="profile-head"><Image className="avatar" src={avatar(player.minecraftUuid)} width={104} height={104} alt={`${player.username}'s Minecraft head`}/><div><h1 style={{fontSize:'clamp(48px,8vw,72px)',margin:0}}>{player.username}</h1><span className={player.online?'online':''}>● {player.online?'ONLINE':'OFFLINE'}</span></div></div><div className="facts"><div><div className="meta">FIRST JOINED</div><b>{date(player.firstJoinedAt)}</b></div><div><div className="meta">LAST SEEN</div><b>{player.online?'Online now':date(player.lastSeenAt)}</b></div><div><div className="meta">PLAYTIME</div><b>{duration(player.playtimeSeconds,true)}</b></div><div><div className="meta">SESSIONS</div><b>{player.sessionCount}</b></div></div></section>}

