'use client'; import { useEffect,useState } from 'react'; import { PlayerCard } from '../../components'; import type { Player } from '../../lib/api';
export function PlayerDirectory({initial}:{initial:Player[]}){const [players,setPlayers]=useState(initial),[search,setSearch]=useState('');useEffect(()=>{const t=setTimeout(async()=>{try{const r=await fetch(`${process.env.NEXT_PUBLIC_API_URL??'http://localhost:3001'}/v1/players?search=${encodeURIComponent(search)}`);if(r.ok)setPlayers(await r.json())}catch{}},180);return()=>clearTimeout(t)},[search]);const online=players.filter(p=>p.online);return <><input className="search" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search players…" aria-label="Search players"/><div className="section"><h2>Online now</h2>{online.length?<div className="players">{online.map(p=><PlayerCard key={p.minecraftUuid} player={p}/>)}</div>:<p className="sub">Nobody is online right now.</p>}</div><div className="section"><h2>Everyone</h2><div className="players">{players.map(p=><PlayerCard key={p.minecraftUuid} player={p}/>)}</div></div></>}

